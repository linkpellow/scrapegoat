╔════════════════════════════════════════════════════════════════╗
║                  SCRAPER PLATFORM - QUICK START                ║
║                      System Ready ✅                            ║
╚════════════════════════════════════════════════════════════════╝

📋 SYSTEM STATUS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ PostgreSQL 16      Running on port 5432
✅ Redis 7            Running on port 6379
✅ Python 3.9.6       All dependencies installed
✅ Node.js 24.5.0     All dependencies installed
✅ Database           7 tables created
✅ Playwright         Chromium browser installed

🚀 START SERVERS (3 TERMINALS REQUIRED)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Terminal 1 - API Server:
┌────────────────────────────────────────────────────────────────┐
│ cd /Users/linkpellow/SCRAPER                                   │
│ source venv/bin/activate                                       │
│ make start                                                     │
└────────────────────────────────────────────────────────────────┘
→ http://localhost:8000      (API)
→ http://localhost:8000/docs (Documentation)

Terminal 2 - Celery Worker:
┌────────────────────────────────────────────────────────────────┐
│ cd /Users/linkpellow/SCRAPER                                   │
│ source venv/bin/activate                                       │
│ make start-worker                                              │
└────────────────────────────────────────────────────────────────┘

Terminal 3 - Web UI:
┌────────────────────────────────────────────────────────────────┐
│ cd /Users/linkpellow/SCRAPER                                   │
│ make start-web                                                 │
└────────────────────────────────────────────────────────────────┘
→ http://localhost:3000 (Web Interface)

🔍 VERIFY SYSTEM
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
source venv/bin/activate && make verify

🧪 TEST SYSTEM
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
curl http://localhost:8000/           # Health check
make test-api                          # Test API endpoints
make test-step-two                     # Test orchestration
make test-step-three                   # Test scraping
make test-step-six                     # Complete API test

🛠️ USEFUL COMMANDS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
make verify        → Verify all dependencies
make infra-up      → Start PostgreSQL + Redis
make infra-down    → Stop PostgreSQL + Redis
make migrate       → Run database migrations
make db-reset      → Reset database (destroys data!)
make clean         → Clean Python cache
make help          → Show all commands

📚 DOCUMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
INSTALLATION_SUMMARY.md    → What was installed
START_SERVERS.md           → Detailed startup guide
READY_TO_TEST.md           → Quick testing reference
SETUP_COMPLETE.md          → Full setup & troubleshooting

🎯 ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Browser → Next.js (3000) → FastAPI (8000) → PostgreSQL (5432)
                              ↓
                         Redis (6379) → Celery Worker
                                             ↓
                                   Scrapy + Playwright

🔑 KEY FEATURES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
• Job Management API
• CSS Selector-based Field Mapping
• List Pattern Detection (List Wizard)
• Preview Mode for Testing
• Background Job Processing
• Multiple Scraping Strategies (Scrapy/Playwright)
• Session Management
• Event Logging & History
• Modern React Web UI

💡 FIRST TIME TESTING?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Start all 3 servers (see above)
2. Open http://localhost:3000 in browser
3. Create your first job via UI or API
4. Configure field mappings
5. Preview extraction
6. Run the job
7. View results

🆘 TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Port in use?
  lsof -ti:8000 | xargs kill -9    # Kill API server
  lsof -ti:3000 | xargs kill -9    # Kill web UI

Docker not running?
  make infra-up

Database connection error?
  docker ps                        # Check if postgres running
  make infra-down && make infra-up # Restart services

Redis connection error?
  docker ps                        # Check if redis running
  make infra-down && make infra-up # Restart services

Verification failed?
  cat SETUP_COMPLETE.md            # Full troubleshooting guide

╔════════════════════════════════════════════════════════════════╗
║          🎉 SYSTEM READY - START TESTING! 🎉                   ║
╚════════════════════════════════════════════════════════════════╝
