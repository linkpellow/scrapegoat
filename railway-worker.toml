[build]
builder = "NIXPACKS"

[deploy]
startCommand = "echo '🔧 Installing system dependencies for Playwright...' && apt-get update -qq && apt-get install -y -qq libglib2.0-0 libnss3 libnspr4 libdbus-1-3 libatk1.0-0 libatk-bridge2.0-0 libcups2 libdrm2 libxcb1 libxkbcommon0 libatspi2.0-0 libx11-6 libxcomposite1 libxdamage1 libxext6 libxfixes3 libxrandr2 libgbm1 libpango-1.0-0 libcairo2 libasound2 > /dev/null 2>&1 && echo '🔧 Installing Playwright browser...' && python -m playwright install chromium && echo '✅ Playwright ready' && echo 'Redis URL:' && echo $APP_REDIS_URL | head -c 50 && echo '...' && echo 'Broker URL:' && echo $APP_CELERY_BROKER_URL | head -c 50 && echo '...' && export APP_DATABASE_URL=$(echo $APP_DATABASE_URL | sed 's|^postgresql://|postgresql+psycopg://|') && celery -A app.celery_app worker --loglevel=info --concurrency=2 --pool=solo"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

